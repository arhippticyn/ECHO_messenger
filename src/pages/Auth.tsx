import Register from "../components/Auth/Register"

interface AuthProps {
    
}

const Auth = ({}: AuthProps) => {
    return (
        <div>
            <Register />
        </div>
    )
}

export default Auth